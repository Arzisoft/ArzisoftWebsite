<nav class="navbar" id="navbar">
  <div class="navbar__inner container">
    <!-- Logo -->
    <a href="/" class="navbar__logo">
      <div class="navbar__logo-icon">a</div>
      <span class="navbar__logo-text">arzisoft</span>
    </a>

    <!-- Desktop links -->
    <div class="navbar__links">
      <a href="/" class="navbar__link <%= currentPage === 'home' ? 'navbar__link--active' : '' %>">Home</a>
      <a href="/product" class="navbar__link <%= currentPage === 'product' ? 'navbar__link--active' : '' %>">Products</a>
      <a href="/services" class="navbar__link <%= currentPage === 'services' ? 'navbar__link--active' : '' %>">Services</a>
      <a href="/ai" class="navbar__link <%= currentPage === 'ai' ? 'navbar__link--active' : '' %>">AI Chat</a>
      <a href="/devgrup" class="navbar__link navbar__link--dim <%= currentPage === 'devgrup' ? 'navbar__link--active' : '' %>">DEVGRUP</a>
      <a href="/ai" class="btn-primary navbar__cta">Get Started</a>
    </div>

    <!-- Mobile toggle -->
    <button class="navbar__toggle" id="navToggle" aria-label="Toggle menu">
      <i data-lucide="menu" style="width:22px;height:22px;"></i>
    </button>
  </div>

  <!-- Mobile menu -->
  <div class="navbar__mobile" id="navMobile">
    <a href="/" class="navbar__mobile-link <%= currentPage === 'home' ? 'navbar__mobile-link--active' : '' %>">Home</a>
    <a href="/product" class="navbar__mobile-link <%= currentPage === 'product' ? 'navbar__mobile-link--active' : '' %>">Products</a>
    <a href="/services" class="navbar__mobile-link <%= currentPage === 'services' ? 'navbar__mobile-link--active' : '' %>">Services</a>
    <a href="/ai" class="navbar__mobile-link <%= currentPage === 'ai' ? 'navbar__mobile-link--active' : '' %>">AI Chat</a>
    <a href="/devgrup" class="navbar__mobile-link <%= currentPage === 'devgrup' ? 'navbar__mobile-link--active' : '' %>">DEVGRUP</a>
    <a href="/ai" class="navbar__mobile-link" style="margin-top:8px;background:var(--text-primary);color:#fff;font-weight:500;">Get Started</a>
  </div>
</nav>

<style>
.navbar {
  position: fixed; top: 0; left: 0; right: 0; z-index: 50;
  background: rgba(255,255,255,0.6);
  backdrop-filter: blur(20px);
  border-bottom: 1px solid transparent;
  transition: background 0.3s, border-color 0.3s;
}
.navbar.scrolled {
  background: rgba(255,255,255,0.85);
  border-bottom-color: var(--border);
}
.navbar__inner {
  height: var(--nav-h);
  display: flex; align-items: center; justify-content: space-between;
}
.navbar__logo { display: flex; align-items: center; gap: 10px; }
.navbar__logo-icon {
  width: 32px; height: 32px; border-radius: 8px;
  background: var(--text-primary);
  display: flex; align-items: center; justify-content: center;
  font-size: 15px; font-weight: 700; color: #fff;
}
.navbar__logo-text {
  font-size: 18px; font-weight: 600; color: var(--text-primary);
  letter-spacing: -0.02em;
}
.navbar__links {
  display: flex; align-items: center; gap: 4px;
}
.navbar__link {
  padding: 8px 16px; border-radius: var(--radius-full);
  font-size: 14px; font-weight: 400; color: var(--text-secondary);
  transition: color 0.2s, background 0.2s;
}
.navbar__link:hover { color: var(--text-primary); }
.navbar__link--active {
  font-weight: 500; color: var(--text-primary);
  background: rgba(0,0,0,0.04);
}
.navbar__link--dim { opacity: 0.5; }
.navbar__cta { margin-left: 16px; padding: 10px 20px; font-size: 14px; }
.navbar__toggle { display: none; color: var(--text-primary); padding: 4px; }
.navbar__mobile {
  display: none; flex-direction: column; gap: 4px;
  padding: 0 24px 24px;
  background: rgba(255,255,255,0.98);
}
.navbar__mobile.open { display: flex; }
.navbar__mobile-link {
  padding: 12px 16px; border-radius: 12px;
  font-size: 15px; font-weight: 400; color: var(--text-secondary);
  transition: background 0.2s, color 0.2s;
}
.navbar__mobile-link:hover { background: rgba(0,0,0,0.03); color: var(--text-primary); }
.navbar__mobile-link--active {
  background: rgba(0,0,0,0.03); font-weight: 500; color: var(--text-primary);
}

@media (max-width: 768px) {
  .navbar__links { display: none; }
  .navbar__toggle { display: block; }
}
</style>
